import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  logo: {
    fontSize: '28px',
    marginRight: '10px',
  },
  wrapper: {
    padding: '10px',
  },
});

export default useStyles;
